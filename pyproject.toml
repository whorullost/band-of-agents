[project]
name = "band-agents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "band-sdk[anthropic,claude-sdk]>=1.0.0",
    "mcp>=1.28.0",
    "python-dotenv>=1.2.2",
    "pyyaml>=6.0.3",
]
